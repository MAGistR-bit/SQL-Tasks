### 💾 Клонирование репозитория

Прежде чем клонировать репозиторий, нужно убедиться, что
установлен Git. В большинстве случаев он уже есть в системе,
но если нет, его можно скачать с официального сайта и установить.

Чтобы проверить, установлен ли Git, открываем терминал
и вводим команду `git --version`. Если Git установлен,
то увидим его версию.

```textmate
git version 2.53.0.windows.1
```

Дальше нужно настроить имя пользователя и email, которые
будут использоваться для коммитов. Для этого используем
такую команду, не забудьте заменить имя и почту на свои:

```textmate
git config --global user.name "Имя"
git config --global user.email "your.email@mail.com"
```

Эти данные будут отображаться в истории коммитов, так что лучше
указать те же, что и в GitHub.

Чтобы посмотреть все глобальные настройки Git, вводим
команду `git config --list --global`. Если нужно изменить
какие-то параметры, можно просто снова
выполнить команду `git config --global` и указать
новые значения.

Далее предлагаю настроить SSH-соединение.
Переходим в свой профиль
`GitHub → Settings → SSH and GPG keys` и нажимаем
`New SSH key`. Там вводим название (например, «Рабочий ноутбук»),
вставляем скопированный ключ в поле `Key` и нажимаем `Add SSH key`.

Теперь проверим, всё ли сработало и установлено ли SSH-соединение.
Для этого вводим команду:

```textmate
ssh -T git@github.com
```

Если всё ок, то получим следующее сообщение:

```textmate
Hi your-nickname! You've successfully authenticated, but GitHub does not provide shell access.
```

Переходим в терминале в папку, где хотим разместить
клонированный проект. Вводим команду клонирования:

```textmate
git@github.com:MAGistR-bit/SQL-Tasks.git
```

После выполнения команды в выбранной папке появится директория `SQL-Tasks`.  
Всё! Теперь можно открыть папку с проектом в редакторе кода и начать работу.

### 💾 Запуск докер-контейнера с базой

Перейдите в терминале в папку с проектом

```textmate
cd папка с проектом
```

Вводим команду

```textmate
docker compose up -d --build
```

Подключение к базе данных

```textmate
docker exec -it sql-tasks-postgres-1 psql -U postgres -d examples_db
```

Иия контейнера (у меня sql-tasks-postgres-1) может быть другим. Чтобы проверить какое у вас, вводим следующую команду

```textmate
docker ps
```

и смотрим имя контейнера в столбце name.
Если все сработало должно появиться examples_db=#, то есть вы в базе данных и можно работать с ней. Команда \dt - список таблиц. Описание таблиц https://www.sql-ex.ru/exercises/index.php?act=learn, где на странице есть ссылка - Схема базы данных
